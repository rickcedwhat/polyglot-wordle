# Seed corpora and filtered wordlists for the dictionary pipeline.
# Cached FrequencyWords downloads (`*_50k.txt`) are gitignored; committed
# `*_words.txt` files are optional snapshots of the filtered 5-letter seeds.

